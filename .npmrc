# Turn an engine mismatch into a refused install with a readable message, rather
# than a warning that scrolls past and resurfaces later as a confusing crash.
#
# The root engines field requires Node >= 20, which matches everything in the
# dependency tree, so this cannot fail an install that would otherwise work.
engine-strict=true
