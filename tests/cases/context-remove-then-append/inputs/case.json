{
  "describes": "remove takes the sections out and keeps the cached copies, so append restores them",
  "steps": [
    { "install": "review-kit", "targets": ["claude-code"] },
    { "context": "remove" },
    { "context": "append" }
  ]
}
