---
description: Read and update the team's JIRA board through its REST API.
---

# JIRA board

Tickets live on the `TEAM` board. A ticket in `In Review` needs a linked pull
request before it can move to `Done`.

This file is shipped, byte for byte, by both team-conventions and sprint-kit —
which is what makes it a *shared* item rather than a conflict.
