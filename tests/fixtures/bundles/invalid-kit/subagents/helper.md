---
tools:
  - Read
---

Mistake one: a subagent with no `description`, so no harness can tell when to
invoke it.

Mistake two: it is called `helper`, and so is the skill next door. On Reasonix
and Pi a subagent *is* a skill, so the two would be filed in the same place.
